// API configuration
export const API_BASE_URL = '/valuation-api';

// You can add other configuration values here as needed
export const config = {
    apiBaseUrl: API_BASE_URL,
};

export default config;